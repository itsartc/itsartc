/**
 * Placeholder landing page.
 *
 * The project was reset to a clean slate; the 3D world is being rebuilt. This
 * page exists so the Vercel deployment keeps building and serving something
 * coherent in the meantime.
 */
export default function Home() {
  return (
    <main className="flex min-h-full items-center justify-center px-6">
      <div className="max-w-md text-center">
        <p className="font-mono text-xs uppercase tracking-[0.3em] opacity-40">itsartc</p>
        <h1 className="mt-4 text-3xl font-semibold">A live professional world.</h1>
        <p className="mt-4 text-sm leading-relaxed opacity-60">
          Rebuilding. The world is being authored in 3D.
        </p>
      </div>
    </main>
  );
}
